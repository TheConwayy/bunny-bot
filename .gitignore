# Git will ignore the following files

# Ignore 'node_modules'
node_modules

# Ignore 'wakatime-project'
.wakatime-project

# Ignore the token so that I can test with ease, and no-one needs that lol
config/token.yml